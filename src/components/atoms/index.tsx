/**
 * Atomic Components Index
 *
 * This barrel export file consolidates all atomic UI components for easy importing
 * throughout the application. Atomic components are the smallest, most basic UI
 * building blocks that can't be broken down further while maintaining their purpose.
 */

/**
 * Reusable button component for user interactions
 * @see ./Button for component implementation
 */
export { default as Button } from "./Button";
